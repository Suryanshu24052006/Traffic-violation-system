\
# Core CV / detection / tracking
ultralytics>=8.3.0
supervision>=0.24.0,<0.31.0   # sv.ByteTrack is deprecated/renamed in 0.31+; pinned until tracker.py is updated
opencv-python-headless>=4.10.0

# ANPR / OCR
easyocr>=1.7.1

# Data / config
numpy>=1.26.0
pandas>=2.2.0
pyyaml>=6.0.1

# Database
# (sqlite3 is stdlib — no install needed)

# Dashboard
streamlit>=1.38.0
plotly>=5.24.0

# Testing
pytest>=8.3.0
